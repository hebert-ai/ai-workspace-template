# Custom Workspace Rules

Add user-specific workflow preferences here.

This file is read after the root workspace files and before project-local instructions. Use it for local overrides, preferred habits, and personal helper conventions that should not modify the template-managed root files.
