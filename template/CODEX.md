# Codex Workspace Adapter

Treat this workspace root as a shared coordination workspace.

Read in order:

1. `AGENTS.md`
2. `WORKSPACE.md`
3. `custom/AGENTS.md` if present

Use the root for planning, shared documentation, and new-project bootstrap. If work becomes specific to one project, switch into `projects/<project>/` and follow that project’s local files.
