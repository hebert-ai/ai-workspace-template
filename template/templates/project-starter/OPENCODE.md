# <PROJECT_NAME> — OpenCode Guide

Read `../../WORKSPACE.md`, `../../custom/AGENTS.md` if present, and `./AGENTS.md`.

Use this folder for the project’s own context and implementation work. Reference the root only for shared policy and workspace coordination.
