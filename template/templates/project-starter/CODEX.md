# <PROJECT_NAME> — Codex Guide

Read `../../WORKSPACE.md`, `../../custom/AGENTS.md` if present, and then `./AGENTS.md`.

This project directory is the correct context for detailed planning and implementation. Shared cross-project rules remain at the workspace root.
